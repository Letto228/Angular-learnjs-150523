import {FilterByProductPipe} from './filter-by-product.pipe';

describe('FilterByProductPipe', () => {
    it('create an instance', () => {
        const pipe = new FilterByProductPipe();

        expect(pipe).toBeTruthy();
    });
});
