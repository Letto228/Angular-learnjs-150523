import {LoadScrollDirective} from './load-scroll.directive';

describe('LoadScrollDirective', () => {
    it('should create an instance', () => {
        const directive = new LoadScrollDirective();

        expect(directive).toBeTruthy();
    });
});
